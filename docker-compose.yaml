version: '3.8'

services:
  postgres-db:
    image: postgres:15
    container_name: tesis-postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: ecommercedb
    ports:
      - "5432:5432"